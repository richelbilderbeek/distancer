sudo: true
language: cpp
compiler: gcc

addons: 
  apt: 
    packages: libboost-all-dev

before_install:
  - sudo add-apt-repository -y ppa:lyx-devel/release; sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test; sudo apt-get update -qq

install: 
  - sudo apt-get install -qq g++-5 lyx texlive pgf

script: 
 - ./build.sh
 - ./create_pdf.sh
 - ls *.pdf
 - ls